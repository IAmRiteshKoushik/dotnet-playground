# C# object and nullability basics demonstrated

The learner created a sealed class with constructor parameters, read-only properties, an expression-bodied method, and a nullable `string?` property checked with `is not null`. This establishes the baseline for introducing `Task<T>`, `await`, and cancellation in the next API-relevant exercise.
