# Async lookup and null fallback demonstrated

The learner completed a `Task<User?>` lookup using `await`, `CancellationToken`, null-conditional access, and the `??` fallback operator. Future API work can introduce framework-provided request cancellation and async handler return values without re-teaching the basic control flow.
