# Minimal API routes verified live

The learner built and ran an ASP.NET Core Minimal API with `WebApplication.CreateBuilder`, `Build`, two `MapGet` routes, route-parameter binding, and JSON `Results.Ok` responses. Both `GET /health` and `GET /greet/Ada` returned HTTP 200 from Kestrel on the local development server.
